<div id="page-content">
  <h1 class="text-center mt-4"><%= "#{@user.username}'s Profile" %></h1>
  <%= gravatar_for @user, size: 100 %>
  <h3 class="text-center mt-4">Article</h3>
  <%= render 'articles/article', resource: @user.articles %>
</div>