<div class="container">
  <div class="row justify-content-center">
    <div class="col-10">
    <%= render 'shared/errors' %>
      <%= form_with(model: article, class: 'shadow p-3 mb-3 bg-white rounded') do |form| %>
        <div class="form-group row">
          <%= form.label :title, class: 'col-2 col-form-label' %>
          <div class="col-10">
            <%= form.text_field :title, class: 'form-control shadow rounded', placeholder: 'Title of article' %>
          </div>
        </div>
        <div class="form-group row">
          <%= form.label :body, class: 'col-2 col-form-label' %>
          <div class="col-10">
            <%= form.text_area :body, rows: 10, class: 'form-control shadow rounded', placeholder: 'Body of article' %>
          </div>
        </div>
        <div class="form-group row justify-content-center">
          <%= link_to 'Cancel', articles_path, class: 'btn btn-danger btn-lg text-center' %>
          <%= form.submit class: 'btn btn-primary btn-lg ml-1' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
